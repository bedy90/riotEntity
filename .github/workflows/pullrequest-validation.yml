# Dependency Review Action
#
# This Action will scan dependency manifest files that change as part of a Pull Request,
# surfacing known-vulnerable versions of the packages declared or updated in the PR.
# Once installed, if the workflow run is marked as required, PRs introducing known-vulnerable
# packages will be blocked from merging.
#
# Source repository: https://github.com/actions/dependency-review-action
# Public documentation: https://docs.github.com/en/code-security/supply-chain-security/understanding-your-software-supply-chain/about-dependency-review#dependency-review-enforcement
name: 'Bedy90 - Riot Entity - PullRequest Check'
on:
  pull_request:
    branches: [ "dev", "prod" ]

# If using a dependency submission action in this workflow this permission will need to be set to:
#
# permissions:
#   contents: write
#
# https://docs.github.com/en/enterprise-cloud@latest/code-security/supply-chain-security/understanding-your-software-supply-chain/using-the-dependency-submission-api
permissions:
  contents: read
  # Write permissions for pull-requests are required for using the `comment-summary-in-pr` option, comment out if you aren't using this option
  pull-requests: write
  security-events: write

jobs:       
  eslint-review:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]
        # language: [ 'javascript', 'typescript' ]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - name: Checkout
      uses: actions/checkout@v4.1.7

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4.0.2
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm ci

    - name: Run lint
      run: npm run eslint
      continue-on-error: false  # Arrête le workflow si le lint échoue      

  build:
    runs-on: ubuntu-latest
    needs: eslint-review
    strategy:
      matrix:
        node-version: [18.x]

    steps:
    - name: Checkout
      uses: actions/checkout@v4.1.7

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4.0.2
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm ci
      
    - name: Run build
      run: npm run compile:npx
      continue-on-error: false  # Arrête le workflow si le build échoue


  audit_solution:
    runs-on: ubuntu-latest
    needs: [eslint-review, build]
    strategy:
      matrix:
        node-version: [18.x]
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.7
    
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4.0.2
        with:
          node-version: ${{ matrix.node-version }}
    
      - name: Install dependencies
        run: npm ci
 
      - name: Run audit
        run: npm audit --audit-level=high
        continue-on-error: false  # Arrête le workflow si le build échoue
      
      # - name: Run CodeQL Analysis
      #   uses: github/codeql-action/init@v3
      #   with:
      #     languages: javascript, typescript
  
      # - name: Perform CodeQL Analysis
      #   uses: github/codeql-action/analyze@v3

  outdated_package:
    runs-on: ubuntu-latest
    needs: [eslint-review, build]
    strategy:
      matrix:
        node-version: [18.x]
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.7
    
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4.0.2
        with:
          node-version: ${{ matrix.node-version }}
    
      - name: Install dependencies
        run: npm ci

      # - name: Install ACTIONS_github
      #   run: npm install @actions/github
            
      # - name: Install ACTIONS_exec
      #   run: npm install @actions/exec
        
      - name: Check outdated dependencies
        # if: success()
        id: outdated
        run: |
          outdated=$(npm outdated --json)
          echo "::set-output name=outdated::$outdated"
          || exit 0
        # run: |
        #  npm outdated --json || exit 0
        continue-on-error: true  # Continuer en cas d'erreur pour diagnostiquer
          
      # - name: Run script to comment on Pull Request
      #   if: steps.outdated.outcome == 'success'
      #   run: node .github/scripts/commentOnPR.js

      - name: Comment on Pull Request
        if: steps.outdated.outcome == 'success'
        uses: actions/github-script@v5
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            async function commentOnPullRequest(github) {
              try {
                const outdated = ${{ steps.outdated.outputs.outdated }}
                console.log('npm outdated result:', outdated);

                // Le reste de votre logique de traitement des résultats de npm outdated

                // Créer le commentaire sur la Pull Request
                await github.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.payload.pull_request.number,
                  body: 'Votre commentaire ici',
                });
              } catch (error) {
                console.error('Error commenting on pull request:', error);
                throw error; // Rejeter l'erreur pour la capturer dans le workflow
              }
            }

            commentOnPullRequest(github).catch(error => {
              console.error('Error commenting on pull request:', error);
              process.exit(1);
            });
          # script: |
          #   async function commentOnPullRequest(github) {
          #     try {
          #       // Exécuter npm outdated --json pour obtenir les dépendances obsolètes
          #       const outdated = require('child_process').execSync('npm outdated --json').toString();
          #       const result = JSON.parse(outdated);

          #       // Organiser les dépendances par niveau de sévérité
          #       const updatesBySeverity = {
          #         major: [],
          #         minor: [],
          #         patch: [],
          #       };

          #       Object.keys(result).forEach(depName => {
          #         const depInfo = result[depName];
          #         const currentVersion = depInfo.current;
          #         const wantedVersion = depInfo.wanted;
          #         const latestVersion = depInfo.latest;

          #         if (isMajorUpdate(currentVersion, wantedVersion)) {
          #           updatesBySeverity.major.push({ name: depName, current: currentVersion, wanted: wantedVersion, latest: latestVersion });
          #         } else if (isMinorUpdate(currentVersion, wantedVersion)) {
          #           updatesBySeverity.minor.push({ name: depName, current: currentVersion, wanted: wantedVersion, latest: latestVersion });
          #         } else if (isPatchUpdate(currentVersion, wantedVersion)) {
          #           updatesBySeverity.patch.push({ name: depName, current: currentVersion, wanted: wantedVersion, latest: latestVersion });
          #         }
          #       });

          #       // Charger le template Markdown depuis le fichier
          #       const templatePath = '.github/template/outdated.md';
          #       const templateContent = await github.repos.getContent({
          #         owner: context.repo.owner,
          #         repo: context.repo.repo,
          #         path: templatePath,
          #       });

          #       const templateBody = Buffer.from(templateContent.data.content, 'base64').toString('utf-8');

          #       // Remplacer les placeholders du template avec les données structurées
          #       const finalComment = Mustache.render(templateBody, updatesBySeverity);

          #       // Créer le commentaire sur la Pull Request
          #       await github.issues.createComment({
          #         owner: context.repo.owner,
          #         repo: context.repo.repo,
          #         issue_number: context.payload.pull_request.number,
          #         body: finalComment,
          #       });
          #     } catch (error) {
          #       console.error('Error commenting on pull request:', error);
          #       process.exit(1);
          #     }
          #   }

          #   // Fonctions utilitaires pour déterminer le niveau de mise à jour
          #   function isMajorUpdate(currentVersion, wantedVersion) {
          #     return currentVersion !== wantedVersion && wantedVersion.startsWith('1');
          #   }

          #   function isMinorUpdate(currentVersion, wantedVersion) {
          #     return currentVersion !== wantedVersion && wantedVersion.startsWith('0');
          #   }

          #   function isPatchUpdate(currentVersion, wantedVersion) {
          #     return currentVersion !== wantedVersion && wantedVersion.startsWith('0.');
          #   }

          #   commentOnPullRequest(github).catch(error => {
          #     console.error('Error commenting on pull request:', error);
          #     process.exit(1);
          #   });
 